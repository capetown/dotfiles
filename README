My dotfiles.

Run "install.sh" to install.
Run "install.sh -f" to destructively install.

TODO
----
Check color ls support for Linux.  Can it be the same as for Mac?
Set vi tabs to 2
Variable email address in gitconfig
Variable home directory in gitconfig
IDE settings (?)
Mozilla settings (?)
appended
